1) What is an operator?
   operators are used for calculation in python
2) Why does input() return text?
   because the input is not specified as an integer, float or boolean.
   and by default the input is string.
3) What does int() do?
   it is used to specify the input as an integer.
   eg: 1, 2, 3, 4.
4) What does float() do?
   it specifies the input as a float.
   eg: 1.3, 2.7.
5) What is the difference between "5" and 5?
   "5" is an input. and when printed it stays the same.
    5 is a variable.
